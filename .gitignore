# Android Studio / Gradle
*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
**/build/
/captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab
*.ap_
*.dex

# Keystores — never commit signing material
*.jks
*.keystore

# Self-signed dev cert for the webapp dev server (scripts/gen_dev_cert.sh
# regenerates it) — not a real secret, but no private key belongs in git.
/webapp/certs/

# Never commit the Claude API key or any local secrets file
secrets.properties
*.env
.env.*
/server/local.properties

# Log/cache
*.log
/app/release/

# Dev-script output
/dist/
/scripts/logs/
/BUILD_NUMBER
